{
    "version": "2.0.0",
    "tasks": [
      {
        "label": "echo",
        "type": "shell",
        "command": "echo ${command:folders.packageJson}"
      },
      {
        "label": "Build current project",
        "type": "shell",
        "command": "heft build --clean",
        "options": {
          "cwd": "${command:folders.packageJson}"
        }
      }
    ]
  }